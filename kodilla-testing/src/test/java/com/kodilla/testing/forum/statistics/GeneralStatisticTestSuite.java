// package com.kodilla.testing.forum.statistics;

import org.junit.Assert;
import org.junit.Ignore;
import org.junit.Test;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;


//public class GeneralStatisticTestSuite {


  //  public void calculateAdvStatisticPosts1(Statistics statictics) {
// gdy liczba komentarzy > liczba postów,
        // Statistics statisticksMock = mock(Statistics.class);  // poprawione
       ///  GeneralStatistic generalStatictic = new GeneralStatistic();

       // generalStatictic.calculateAdvStatistics();
       // statisticksMock.showStatistic();

      //  when(statisticsMock.postCount()).thenReturn(100);
        //when(statisticsMock.commentCount()).thenReturn(500);
       // when(statisticsMock.userNames()).thenReturn(userNames); *//

        //When
      //  double getPostAvgPerUser = statisticksMock.getPostAvgPerUser();
      //  double getCommentsAvgPerUser = statisticksMock.getCommentsAvgPerUser();
      //  double getCommentsAvgPerPost = statisticksMock.getCommentsAvgPerPost();

        //Then
      //  Assert.assertEquals(10, postAvgPerUser, 0);
      //  Assert.assertEquals(50, commentsAvgPerUser, 0);
      //  Assert.assertEquals(5, commentsAvgPerPost, 0);
  //  }


   /*
public void calculateAdvStatisticPosts2(Statistics statictics) {
gdy liczba komentarzy < liczba postów,
}
}
*/